"""Sync failing production traces into the eval dataset.

DEPRECATED: Use the Databricks notebook instead:
    notebooks/sync_failing_traces.py

Deploy it with:
    python scripts/deploy_notebooks.py
"""
